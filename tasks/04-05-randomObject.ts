/**
 * 4th task is to implement package manager, install typescript, name your project and set start scripts (start, dev) using ts-node and ts-node-dev
 *
 * 5th task is to add the faker package and use it to generate your object with some random details, then print it.
 * Result should be printed into the console after running npm start command.
 * This package will require typescript extension. Google how to use faker package with typescript and use missing dependencies in your project.
 */
